{
  "name": "@axonmind/types",
  "version": "0.1.0",
  "private": false,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'Types are source-only; generated by ts-rs from Rust types'",
    "typecheck": "tsc --noEmit",
    "test": "bun --bun vitest run"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3"
  }
}
