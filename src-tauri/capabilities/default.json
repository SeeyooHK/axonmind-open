{
  "identifier": "default",
  "description": "Default capability — grants all axonmind plugin commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "axonmind:default",
    "dialog:allow-open"
  ]
}
